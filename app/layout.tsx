import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TUTUCA - La Plataforma #1 de Influencer Marketing en LATAM",
  description: "Conectamos marcas con influencers auténticos. La plataforma todo-en-uno para gestionar campañas de influencer marketing con resultados medibles y ROI garantizado.",
  keywords: "influencer marketing, marcas, influencers, LATAM, marketing digital, campañas",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
